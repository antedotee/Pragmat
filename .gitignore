# dependencies
node_modules

# build output
dist
dist-ssr
*.local

# rust / tauri (build artifacts only — Cargo.toml & Cargo.lock MUST be committed)
src-tauri/target

# tauri-generated mobile icons — this app ships desktop only (tauri.conf.json
# references the desktop icons only). Regenerate with `tauri icon` if you ever
# target Android/iOS.
src-tauri/icons/android/
src-tauri/icons/ios/

# logs
logs
*.log
npm-debug.log*

# editor / os
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.sw?

# local AI / tooling caches (not app source)
.claude/
.agents/
skills-lock.json

# reference clone — its own git repo, not part of this app
calendarcn/

# local notes (delete these two lines if you want them in the repo)
DESIGN.md
CLAUDE.md

reference/
vercel.com/
